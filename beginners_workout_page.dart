import 'package:flutter/material.dart';

class BeginnersWorkoutPage extends StatelessWidget {
  const BeginnersWorkoutPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Beginner'),
      ),
    );
  }
}